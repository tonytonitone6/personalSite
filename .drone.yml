clone:
  git:
    image: plugins/git
    depath: 50
    tags: true


pipeline:
    backend:
      image: node:11.13.0
      commands:
        - echo "backend testing"
    front:
      image: node:11.13.0
      when:
        branch: [master, dev]
      commands:
        - yarn install
        - yarn run prod
        - yarn run serve
